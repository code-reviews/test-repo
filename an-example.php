<?php

echo 'This is a bit of PHP code';
echo 'Diffs should have some code';
echo 'This PR should provide some code to evaluate';
echo '... so that we can actually estimate something for it';

echo 'That would be nice, no?';

foreach (range(0, 2000) as $value) {
    echo sprintf('Hello %s!', $value);
}


